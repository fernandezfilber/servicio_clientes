export class Cliente{
    constructor ( id, nombre, year, direccion, telfono)
{

    this.id=id;
    this.nombre=nombre;
    this.year=year;
    this.direccion=direccion;
    this.telfono=telfono;
}

}

module.exports = Cliente;